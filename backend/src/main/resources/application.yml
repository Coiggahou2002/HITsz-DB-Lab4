spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  profiles:
    active: dev
  datasource:
    # 数据库配置
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cat_platform?useUnicode=true&characterEncoding=utf-8
    username: coiggahou
    password: 123456
server:
  port: 9000 # 服务端口号